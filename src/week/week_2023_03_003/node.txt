1.
    让相同元素最多的元素作为组数。然后看看 长度除以组数是不是 大于等于k、。
2.贪心
    排序，前k-1个小的 每一个单独一组。后面剩下的大的元素在一组。

3.遇到子数组问题：基本上就是：每个位置开头怎么样或者每个位置结束怎么样来分析。。
    k =8 以当前数结尾的子数组，总共累加和的模是 6 ，整体是的余数是7
       那么需要找之前的 （6-7 +8）%8

4.嵌套递归问题
    整个流程设计很重要。

5.
    线段树，首先对时间离散化。然后处理车库，再在线段树中去加，线段树中表示的是 [23,34] 这个期间的车辆数 >= 3，就在线段树中 23到34范围加1.